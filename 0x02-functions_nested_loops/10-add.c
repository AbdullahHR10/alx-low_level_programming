#include "main.h"

/**
 * add - adds two integers and returns the result
 * @n: int to be added to another
 * @c: int to be added to another
 * Return: (c + n)
 */

int add(int c, int n)
{
	int i;

	i = c + n;

	return (i);
}
