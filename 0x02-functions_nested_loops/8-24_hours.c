#include "main.h"

/**
 * jack_bauer - Prints every minute of the day of Jack Bauer
 *
 * Return: Always 0
 */

void jack_bauer(void)
{
	int h, m;

	if (m < 60, m++)
	putchar(m);
	putchar('\n');

	if (h < 24, h++)
	putchar(h);
	putchar('\n')
}
