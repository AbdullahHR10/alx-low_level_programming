#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @i: int
 * Return: value of i
 */

int _abs(int)
{
	int c;

	if (i < 0)
	{
		c = i * -1;
	}
	else
	{
		c = i;
	}
	return (c);
}
