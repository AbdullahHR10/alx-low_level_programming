#include <stdio.h>
#include "main.h"

/**
 * main - prints it's own name
 * @argc: an int
 * @argv: an array
 * Return: always 0
 */

int main(int argc, char *argv[])
{
		printf("%s\n", argv[argc * 0]);

		return (0);
}
